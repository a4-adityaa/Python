
def sum(a,b):
    """This function takes two numbers and returns their sum.
       we will take two number:
       -a
       -b
       after that we will use + operator to sum both the numbers
       which return a+b"""
    return a + b

print(sum.__doc__)